/* stuff lol */

int q;

int fact(int n) {
    if (n == 0)
        return 1;
    else
        return n*fact(n-1);
}

void func(void) {
    return;
}

void main(void) {
    int x;
    int y;
    int *z;
    x = 1;
    while (x < 10) {
        int p;
        write(x);
        write(fact(x));
        writeln();
        x = x + 1;
    }
    {int qq;}
    x = y = -1;
    x = x * y;
    z = &(x);
}

int y;
