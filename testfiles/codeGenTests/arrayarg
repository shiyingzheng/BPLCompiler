void f(int A[]) {
    write(A[0]);
}

void main(void) {
    int A[5];
    A[0] = 5;
    A[1] = 3;
    A[2] = 4;
    A[3] = 6;
    A[4] = 3;
    f(A);
}

/*
when passing array, get address, push onto stack
if array was already a param, just copy arg instead of ->
*/
